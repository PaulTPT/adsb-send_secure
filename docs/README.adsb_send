This is the adsb_send-write-a-block package meant as a guide to building
out-of-tree packages. To use the adsb_send blocks, the Python namespaces
is in 'adsb_send', which is imported as:

    import adsb_send

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(adsb_send)
